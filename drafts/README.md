Content that is written but not yet published. Files here are outside `src/content`, so the build ignores them. Move a file back into the matching `src/content/` folder to publish it.

- `schedules/2027.yaml`: a tentative day-by-day outline for the 2027 school, derived from the grant's example agenda. Held back until the schedule is confirmed.
